<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Alarms do not survive a reboot; BootReceiver re-arms them. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application>
        <meta-data
            android:name="org.godotengine.plugin.v2.JetletNotify"
            android:value="com.eternalsky.jetletnotify.JetletNotifyPlugin" />

        <receiver
            android:name="com.eternalsky.jetletnotify.AlarmReceiver"
            android:exported="false" />

        <receiver
            android:name="com.eternalsky.jetletnotify.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
